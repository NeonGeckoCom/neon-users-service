log_level: INFO
logs:
  level_overrides:
    error:
      - pika
    warning:
      - filelock
      - watchdog
      - httpcore
    info:
      - asyncio
    debug: []
MQ:
  server: neon-rabbitmq
  port: 5672
  users:
    mq_handler:
      user: neon_api_utils
      password: Klatchat2021
neon_users_service:
  module: sqlite
  sqlite:
    db_path: /data/neon-users-db.sqlite